#include <stdio.h>
#include <math.h>

int main(void){
    printf("Å„%d = %f\n%f * %f = %f\n",100,sqrt(100),sqrt(100),sqrt(100),sqrt(100) * sqrt(100));
    printf("Å„%d = %f\n%f * %f = %f\n",2,sqrt(2),sqrt(2),sqrt(2),sqrt(2) * sqrt(2));
    return 0;
}